{
  "name": "cognabright-public-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "typecheck": "node --check script.js && node --check i18n/core.js && node --check i18n/boot.js && node --check api/interest.js && node --check api/country.js",
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
